# Nextflow work/cache directories
work/
.nextflow/
.nextflow.log*

# Pipeline outputs (regenerated by running the pipeline)
results/
results_somatic/

# Test data (fetched per README instructions; not committed to keep the repo
# small and avoid redistributing third-party files)
test_data/

# Environment / secrets / local overrides
.env
*.env
.envrc
*secrets*

# OS cruft
.DS_Store

# Editors
.vscode/
.idea/
